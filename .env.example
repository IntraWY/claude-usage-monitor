# ⚠️ VAPID Keys — Generate ONCE only with: node -e "const wp=require('web-push'); const k=wp.generateVAPIDKeys(); console.log(k)"
# Never regenerate — all iPhone subscriptions become invalid permanently
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:intrapeawg01@gmail.com

# Upstash Redis — https://console.upstash.com (free tier, no credit card needed)
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Upstash QStash — same console as Redis, under "QStash" tab
QSTASH_TOKEN=
QSTASH_CURRENT_SIGNING_KEY=
QSTASH_NEXT_SIGNING_KEY=

# Your Vercel deployment URL (no trailing slash)
SITE_URL=https://your-project.vercel.app
